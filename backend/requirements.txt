fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.1

# Vector DB
qdrant-client==1.11.1

# Embeddings (runs locally, free, no API key, ONNX-based — no PyTorch, much lighter memory footprint)
fastembed==0.4.2

# LLM (Groq free tier)
groq==0.11.0
httpx==0.27.2

# Document parsing — PyMuPDF gives real font-size/style info (used for PDF
# heading detection), which plain pypdf/markdown-it-py can't provide.
pymupdf==1.28.0
python-docx==1.1.2

# Utilities
tenacity==9.0.0
